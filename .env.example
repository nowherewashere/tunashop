# !!! WARNING: This minimal .env.example file provides a template for configuring the Remnashop.
# Full configuration options and instructions can be found in the official documentation: 
# https://remnashop.mintlify.app/docs/en/install/environment-variables


# Domain through which your bot is publicly accessible.
# Used for receiving webhooks from services (e.g., Telegram, payment systems).
# !!! WARNING: Specify domain WITHOUT HTTP/HTTPS and WITHOUT trailing slash.
APP_DOMAIN=change_me

# Encryption key for securing sensitive data (e.g., payment tokens) in the database.
# !!! CRITICALLY IMPORTANT: Never share this token publicly.
APP_CRYPT_KEY=change_me

# Telegram Bot API token. Obtain it from https://t.me/BotFather/
# !!! CRITICALLY IMPORTANT: Never share this token publicly. It grants full control over your bot.
BOT_TOKEN=change_me

# Secret token for webhook verification. Ensures webhook requests originate from Telegram.
BOT_SECRET_TOKEN=change_me

# Owner Telegram user ID. Grants elevated permissions.
BOT_OWNER_ID=change_me

# Telegram username of the support account used for deep-linking (without @).
BOT_SUPPORT_USERNAME=VPNTuna_support

# Controls the behavior of the "connect" button:
# - empty or false  -> opens the subscription page in the browser
# - true            -> opens the subscription page in WebApp
# - URL (e.g., https://example.com/) -> opens the specified URL in WebApp
# To use the subscription page, deploy the remnawave-subscription-page:
#   https://docs.rw/docs/install/remnawave-subscription-page
# To use a custom subscription page (Orion):
#   https://github.com/legiz-ru/Orion
# To use the Mini App (Maposia):
#   https://github.com/maposia/remnawave-telegram-sub-mini-app
BOT_MINI_APP=false

# Hostname or Docker service name for connecting to the Remnawave API.
# Without HTTP/HTTPS and without trailing slash.
REMNAWAVE_HOST=remnawave

# API token for authenticating with the Remnawave service.
# Created in the panel: Settings → API Tokens.
REMNAWAVE_TOKEN=change_me

# Secret token for verifying incoming webhooks from Remnawave.
# In the Remnawave Panel, this corresponds to WEBHOOK_SECRET_HEADER (env).
REMNAWAVE_WEBHOOK_SECRET=change_me

# Password for the database.
DATABASE_PASSWORD=change_me



# Optional settings with default values (uncomment and modify as needed):
# APP_HOST="0.0.0.0"
# APP_PORT=5000
# APP_LOCALES="ru"
# APP_DEFAULT_LOCALE="ru"
# APP_ASSETS_DIR="/app/assets"
# APP_ORIGINS=""
# APP_SWAGGER_ENABLED=false

# --- Website / web cabinet (public API under /api/v1/public) ---
# Enable the public web API (register/login/subscription/plans/referral).
# When true, APP_JWT_SECRET and APP_API_KEY are REQUIRED (and must differ from APP_CRYPT_KEY).
# WEB_ENABLED=false
# APP_JWT_SECRET=change_me
# APP_API_KEY=change_me
# Public site URL (used for bot -> cabinet links).
# WEB_CABINET_URL="https://v-tuna.com"

# Cloudflare Turnstile captcha on passwordless code requests. Secret is server-side;
# the site key is public and served to the frontend via /api/v1/public/config.
# Leave the secret unset to disable the captcha.
# TURNSTILE_SECRET=change_me
# TURNSTILE_SITE_KEY=change_me

# --- OAuth social sign-in ---
# Authorization-code flow with PKCE, run entirely server-side: the SPA is a static
# export with no server of its own, and the client secret must never reach a browser.
# Ships OFF — with no provider enabled /api/v1/public/config reports an empty provider
# list (the SPA renders no buttons) and /auth/oauth/* answers 404.
#
# The redirect URI registered in the provider's console is EXACTLY:
#   <OAUTH_PUBLIC_BASE_URL>/api/v1/public/auth/oauth/<provider>/callback
# Providers compare it byte-for-byte: no trailing slash, no query string.
#
# OAUTH_PUBLIC_BASE_URL must be the SITE origin, never the API host: the auth cookies
# are host-only (set_auth_cookies sets no domain=), so a callback landing anywhere else
# would set the session on the wrong host and the SPA would never see it. It has its own
# variable rather than deriving from WEB_CABINET_URL because the public domain rotates
# under blocking, and the new redirect URI must be registered with the provider BEFORE
# traffic moves — so the two need to be rotatable independently. Unset => falls back to
# WEB_CABINET_URL.
#
# DOMAIN ROTATION ORDER: add the new redirect URI in the provider console alongside the
# old one -> switch OAUTH_PUBLIC_BASE_URL -> remove the old URI a week later.
# OAUTH_PUBLIC_BASE_URL="https://v-tuna.com"

# Google Cloud Console -> Clients -> OAuth client ID (Web application).
# Scopes are openid + email + profile ONLY: those are non-sensitive, so the app needs
# no Google verification review. Adding any other scope triggers a multi-week review.
# Do NOT upload an app logo in the consent screen either — that starts brand
# verification, and omitting it blocks nothing.
# OAUTH_GOOGLE_ENABLED=false
# OAUTH_GOOGLE_CLIENT_ID=""
# OAUTH_GOOGLE_CLIENT_SECRET=""

# How long a started flow may sit on the consent screen before its state expires.
# OAUTH_STATE_TTL_SECONDS=600
# OAUTH_HTTP_TIMEOUT_SECONDS=10
# Per-IP budgets (one sign-in spends two requests: start + callback). The sharp edge is
# the nginx `tuna_auth` zone in front of these routes.
# OAUTH_START_MAX_PER_IP=30
# OAUTH_CALLBACK_MAX_PER_IP=30
# OAUTH_RATE_WINDOW_SECONDS=600

# --- Referral program (money commission) & payouts ---
# Optional marketing site that maps /r/<code> to the bot referral link (2nd link).
# REFERRAL_SITE_URL=""
# Commission rate in basis points (5000 = 50.00%) and minimum crypto payout
# (kopecks; 100000 = 1000 ₽). The referred-friend trial length is NOT set here — it is
# a trial Plan with availability=INVITED (picked automatically for referred users).
# REFERRAL_RATE_BP=5000
# REFERRAL_PAYOUT_MIN_KOP=100000
# Payout execution: operator (beta, human settles from the bot admin queue) | auto (future).
# PAYOUT_MODE=operator
# PAYOUT_CRYPTO_ASSET=USDT
# PAYOUT_CRYPTO_NETWORK=TRC20
# Weekly Monday 09:00 crypto batch.
# PAYOUT_BATCH_CRON="0 9 * * 1"
# Telegram Stars payout (buy-and-gift; spec §7.2). Stars are spendable INSIDE
# Telegram only (not fiat). Ships OFF: enable only after funding a treasury account
# and setting STARS_RUB_RATE to the real per-Star sourcing cost. Operator mode is
# ToS-safe (a human gifts from the treasury); mtproto_auto is future (D7 sign-off).
# STARS_PAYOUT_ENABLED=false
# Kopecks per 1 Star (retail sourcing cost), frozen on the row at request. e.g. 200 = 2 ₽/⭐.
# STARS_RUB_RATE=200
# Small floor to request a Stars payout (kopecks; 10000 = 100 ₽). No 1000 ₽ gate.
# STARS_MIN_KOP=10000
# Fragment-funded account holding/gifting the Stars float.
# STARS_TREASURY_ACCOUNT=""
# fragment (via crypto/TON) — preferred | appstore.
# STARS_SOURCING=fragment
# operator (beta) | mtproto_auto (future, D7).
# STARS_MODE=operator

# --- Unified support (site + bot -> operator forum topics) ---
# Operators answer website AND bot users from ONE Telegram supergroup, one topic per
# user. Ships OFF: while disabled the bot keeps the BOT_SUPPORT_USERNAME contact link
# and the site widget shows the same fallback. To enable: create a supergroup with
# Topics on, add the bot as admin with "Manage Topics", and disable the bot's group
# privacy in BotFather (/setprivacy -> Disable) so it receives operators' messages.
# SUPPORT_ENABLED=false
# The forum-enabled operator supergroup id (negative, -100...). Required when enabled.
# SUPPORT_GROUP_ID=-1000000000000
# Auto-close a conversation this many minutes after the user's last message (0 = never).
# SUPPORT_IDLE_CLOSE_MINUTES=60

# --- Email delivery (verification / passwordless login codes) ---
# EMAIL_ENABLED=false
# Dev/local only: log the code instead of sending via SMTP (NEVER enable in production).
# EMAIL_CONSOLE=false
# EMAIL_HOST=""
# EMAIL_PORT=587
# EMAIL_USE_TLS=true
# EMAIL_USE_SSL=false
# EMAIL_USERNAME=""
# EMAIL_PASSWORD=""
# EMAIL_FROM_EMAIL=""
# EMAIL_FROM_NAME=""
# EMAIL_VERIFICATION_CODE_TTL_MINUTES=15
# Rate limits on passwordless code requests (per window). Per-IP is generous for
# shared/CGNAT clients; captcha + per-email are the tighter gates.
# EMAIL_CODE_MAX_PER_EMAIL=5
# EMAIL_CODE_MAX_PER_IP=60
# EMAIL_CODE_RATE_WINDOW_SECONDS=3600

# BOT_PROXY_URL=""
# BOT_RESET_WEBHOOK=false
# BOT_DROP_PENDING_UPDATES=false
# BOT_SETUP_COMMANDS=true
# BOT_USE_BANNERS=true

# DATABASE_HOST="remnashop-db"
# DATABASE_PORT=5432
# DATABASE_NAME="remnashop"
# DATABASE_USER="remnashop"
# DATABASE_ECHO=false
# DATABASE_ECHO_POOL=false
# DATABASE_POOL_SIZE=10
# DATABASE_MAX_OVERFLOW=20
# DATABASE_POOL_TIMEOUT=30
# DATABASE_POOL_RECYCLE=1800

# LOG_TO_FILE=true
# LOG_LEVEL="DEBUG"
# LOG_ROTATION="100MB"
# LOG_COMPRESSION="zip"
# LOG_RETENTION="3 days"

# REDIS_HOST="remnashop-redis"
# REDIS_PORT=6379
# REDIS_NAME="0"
# REDIS_PASSWORD=""

# REMNAWAVE_CADDY_TOKEN=""
# REMNAWAVE_COOKIE=""

# REMNAWAVE_CF_CLIENT_ID=""
# REMNAWAVE_CF_CLIENT_SECRET=""

# Guided onboarding funnel (optional). Turn the flow ON/OFF at runtime from the
# dashboard: Dashboard -> Remnashop -> Extra -> Onboarding. The values below only
# customize it; all have sane defaults.
# ONBOARDING_HAPP_LINK_IOS="https://apps.apple.com/app/happ-proxy-utility/id6504287215"
# ONBOARDING_HAPP_LINK_ANDROID="https://play.google.com/store/apps/details?id=com.happproxy"
# ONBOARDING_HAPP_LINK_WINDOWS="https://github.com/Happ-proxy/happ-desktop/releases"
# ONBOARDING_HAPP_LINK_MAC="https://apps.apple.com/app/happ-proxy-utility/id6504287215"
# Apple-only alternative client (INCY), shown when Happ is unavailable in the RU App Store.
# ONBOARDING_HAPP_LINK_IOS_RU="https://apps.apple.com/us/app/incy/id6756943388"
# ONBOARDING_REFRESH_VIDEO_URL=""
# ONBOARDING_HAPP_IMPORT_TEMPLATE="happ://add/{sub_url}"
# ONBOARDING_INCY_IMPORT_TEMPLATE="incy://add/{sub_url}"
# ONBOARDING_NUDGE_DELAYS_HOURS="0.5,3,24"
# ONBOARDING_NUDGE_MIN_GAP_MINUTES=180
# ONBOARDING_NUDGE_DAILY_CAP=4
